export interface listEntry {
  title: string;
  id: string;
  score: string;
  poster: string;
  year: string;
  rating: string;
  notes: string;
}

export interface userAuthData {
  username?: string;
  user: boolean;
}
